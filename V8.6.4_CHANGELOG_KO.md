# V8.6.4 SAFE MERGE 변경 내역

## 채택한 팀원 아이디어
- track canonical flood-stage state 유지
- `track_state_lock`으로 geometry/stage mailbox 동시 갱신 보호
- `stage_conf`와 `vehicle_conf` 분리
- 실제 best.pt 차량 유형 라벨 표시
- CUDA 표시용 velocity projection

## 안전 조정
- 팀원안의 projection 최대 0.25초를 0.12초로 축소
- projection은 렌더링 복사본에서만 수행하며 `last_associated_detections`에 쓰지 않음
- GPU missed-track 정책은 V8.6.3 그대로 0 유지
- 저신뢰 야간 차량을 살리기 위해 `.env` `VEHICLE_DETECTION_CONFIDENCE=0.001` 유지
- temporal visibility hard gate/0.25·0.85 정책은 병합하지 않음
- live CCTV 중 background GPU opportunistic 실행은 병합하지 않음

## 그대로 보존
- 중앙 GPU scheduler와 micro-batch/adaptive cadence
- cuDNN benchmark=False
- V8.6.3 DEM 고도별 침수심 색상 및 수면 마스크
- FULL 모델 순서: best.pt → tire_level.pt → 필요 시 car_flood_cls.pt
- 단계 변화 시에만 하단 침수 알림 표시

## 패키징 안전성
- 팀원 ZIP의 실제 API 키를 가져오지 않음
- 팀원 DB/캐시/__pycache__를 가져오지 않음
- requirements.txt를 V8.6.3 기준 그대로 유지
