# V8.5.32 FULL-DIRECT

- `/api/stage`에서 best.pt 검출 7개가 확인되는데 화면 박스가 보이지 않는 문제를 전송/렌더 경로 문제로 확정.
- WebSocket JPEG 헤더에 최신 detection bbox/track/conf/stage metadata를 동봉.
- 브라우저 canvas가 JPEG를 그린 직후 bbox를 벡터로 직접 오버레이. raw fallback이어도 박스 유지.
- 차량 영상 전송과 AI 박스 렌더를 분리해 잔여 렉 감소.
- detector 결과 갱신 시 `latest_result_at`도 갱신하도록 수정.
- worker status에 detection count와 annotated/focus interest 상태 추가.
- 기존 server-side annotated JPEG도 유지하여 HTTP 경로 호환성 보존.
