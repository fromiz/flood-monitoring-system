# V8.5.31 FULL-DIRECT

## 핵심 변경
- 차량 박스 가시성을 tire_level / car_flood_cls / 추적 확인 로직과 완전히 분리했습니다.
- best.pt가 반환한 raw box는 즉시 그립니다.
- full-frame 1280px 저임계 검출 + 2x2 타일 rescue를 사용합니다.
- 야간/저조도에서 raw box가 0개이면 CLAHE 향상 프레임으로 detector-only 재시도합니다.
- 박스의 stage 판정 실패/지연은 박스 자체를 지우지 않습니다.
- 0개일 때 PowerShell에 frame 크기/평균 밝기/imgsz/device를 포함한 DIRECT 로그를 남깁니다.

- FULL 빌드는 best -> crop -> tire_level -> tire 미검출 시 car_flood_cls 순서를 유지합니다.
